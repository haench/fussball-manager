# Plan: Transfermarkt Game Loop — Fussball Manager

---

## UI Flow

Drei separate Screens, alle folgen dem gleichen `mobile-shell--roster`-Muster wie andere Club-Screens:

```
Club Hub
  └─[Kachel "Transfermarkt"]
       └─► SCREEN 1: Transfermarkt Home (currentScreen = "transferMarket")
            ├─ section.screen.screen--club.screen--transfer-market
            ├─ header.club-header.club-header--roster.glossy-panel
            │    ├─ eyebrow: "Transfermarkt"
            │    ├─ h1: Club-Name
            │    └─ p: "Spieler kaufen und verkaufen"
            ├─ Message-Box (glossy-panel) mit max. 5 Log-Einträgen + externe Angebot-Buttons
            ├─ Zwei Buttons nebeneinander: "Verkaufen" | "Kaufen"
            └─ [Zurück] → Club Hub
            │
            ├─[Button "Verkaufen"]
            │    └─► SCREEN 2: Verkaufen (currentScreen = "transferMarket-sell")
            │         ├─ section.screen.screen--club.screen--transfer-market
            │         ├─ header.club-header.club-header--roster.glossy-panel
            │         │    ├─ eyebrow: "Verkaufen"
            │         │    └─ p: "Spieler zum Verkauf anbieten"
            │         ├─ "Startelf"-Sektion: roster-group → roster-row pro Spieler mit "Verkaufen"-Knopf
            │         ├─ "Bank"-Sektion: roster-group → roster-row pro Spieler mit "Verkaufen"-Knopf
            │         └─ [Zurück] → SCREEN 1
            │
            └─[Button "Kaufen"]
                 └─► SCREEN 3: Kaufen (currentScreen = "transferMarket-buy")
                      ├─ section.screen.screen--club.screen--transfer-market
                      ├─ header.club-header.club-header--roster.glossy-panel
                      │    ├─ eyebrow: "Kaufen"
                      │    └─ p: "Spieler von anderen Vereinen kaufen"
                      ├─ Message-Box mit Log-Einträgen
                      ├─ Liste von max. 5 Spielern (Name, Alter, Stärke, Ablöse)
                      └─ [Zurück] → SCREEN 1
```

- **SCREEN 1**: Header oben, darunter Message-Box (gleiche `glossy-panel` wie training-feed), darunter die zwei Buttons nebeneinander in einem `roster-actions`-Container, ganz unten der Zurück-Knopf.
- Externe Angebote erscheinen als Text in der Message-Box; Annahme/Ablehnung erfolgt über extra Buttons in der Box auf SCREEN 1.
- Nach Annahme/Ablehnung bleibt die Anwendung auf SCREEN 1.
- **SCREEN 2 + 3**: Gleiches Header-Muster wie SCREEN 1, alle Buttons (einschl. Zurück) verwenden `action-button action-button--blue roster-action`, zurück erfolgt mit `setScreen("transferMarket")`.
- **Alle Screens** verwenden CSS-Klassen, die bereits aus dem restlichen Codebase existieren: `screen--club`, `mobile-shell--roster`, `glossy-panel`, `club-header`, `club-header--roster`, `eyebrow`, `roster-actions`, `action-button`, `action-button--blue/--green`, `roster-group`, `roster-row`. Keines der vorhandenen Klassen muss verändert werden. Nur neue IDs werden ergänzt: `screen--transfer-market`, `transfer-market-home-message` etc.

---

## Current "Game Loops" Identified

Implementierte, bereits vorhandene Schleifen, nach deren Muster gearbeitet wird:

| Loop | Datei | Muster |
|---|---|---|
| Match-Simulation | `gameLoop.js` `runMatchLoop()` | Aktives Intervall (`setInterval` 33 ms, fixed-timestep Akkumulator) |
| Post-Match-Phase | `gameLoop.js` `continueAfterMatch()` | Einmaliger Aufruf nach Spielende, ruft nacheinander `processTrainingAfterMatch`, `processYouthAfterMatch`, `progressStadiumUpgrades` |
| Tages-Progression | `leagueWorld.js` `completeMatchDay()` | Einmaliger Aufruf pro Spieltag in Ligawelt |
| Training-Upgrade | `progressTrainingFacilityUpgrade()` | Dekrementiert `daysRemaining` pro Tag, schließt bei 0 ab |
| Jugend-Upgrade | `progressYouthAcademyUpgrade()` | Gleiches Muster |
| Stadion-Upgrade | `progressStadiumUpgrades()` | Gleiches Muster |

**Transfermarkt ist KEINE weiterlaufende aktive Schleife.** Sie ist ein **zustandsgesteuerter, einzelaufruf-artiger Mechanikblock**, der in der Post-Match-Phase aufgerufen wird — exakt wie Jugend, Training und Stadion-Upgrades.

---

## Neue Daten

### Team-Feld (neu)

```js
transferMarket: {
  externalOffer: null | {
    id: string;               // eindeutige Angebots-ID
    buyerClubName: string;    // z. B. "SV Grün-Weiß"
    playerName: string;       // Name des zum Verkauf stehenden Spielers
    offerAmount: number;      // gebotener Preis, ~80 % des Marktwerts
    createdDay: number;       // Spieltag, an dem das Angebot erstellt wurde
    expiresDay: number        // Spieltag, an dem das Angebot ungültig wird
  }
}
```

### Spieler-Felder (neu, pro Spieler)

```js
marketValue: 50000;        // berechnet als strength² × 100, gerundet auf 1000
isOnTransferList: false;   // Spieler steht auf der Verkaufsliste
```

Preisformel:
```
marketValue = Math.round(strength * strength * 100 / 1000) * 1000
```
Später ligaabhängig.

---

## Logzeile (Ereignisanzeige)

Die Transfermarkt-Ansicht zeigt immer eine Logzeile unterhalb der Tab-Inhalte mit maximal **5 Einträgen**, neueste zuerst. Eintragstypen:

| Eintragstyp | Beispieltext |
|---|---|
| `bought` | `Max Mustermann für 12.000 € gekauft.` |
| `listed` | `Tim Becker steht jetzt zum Verkauf.` |
| `unlisted` | `Tim Becker wieder von der Liste genommen.` |
| `external_offer` | `SV Grün-Weiß bietet 80.000 € für Tim Becker.` |
| `accepted` | `Angebot für Tim Becker von SV Grün-Weiß angenommen (80.000 €).` |
| `rejected` | `Angebot für Tim Becker abgelehnt.` |
| `expired` | `Angebot für Tim Becker ist abgelaufen.` |

Log-Daten: `gameState.transferMessages` — Array von maximal 5 Einträgen `{ key, text }`. Wird in Thread-Local Storage persistiert. Alte Einträge unten abschneiden, neue oben (`unshift`).

---

## Externe Angebote — Detail

- Im Post-Match-Loop prüft die Funktion `processTransferMarketLoop(state)` pro Spieltag mit **10 % Wahrscheinlichkeit**, ob ein Angebot generiert wird.
- Angebot wird erstellt, falls `gameState.transferMarket.externalOffer` nicht existiert (es gibt höchstens ein aktives Angebot gleichzeitig).
- Käufer-Club: zufälliger **verein des gleichen Ligazweigs** (nur echte Liga-Clubs, keine Reserve-Teams).
- Angebotsbetrag: **Marktwert × 0,8**, immer auf 1000 gerundet.
- Angebot ist **1 Spieltag gültig** `expiresDay = state.currentDay + 1` → wird am Folgetag automatisch verworfen und als `expired`-Eintrag in die Log geschrieben.
- Angebot erscheint sofort als neuester Eintrag in der Logzeile sowohl im **Kaufen**- als auch im **Verkaufen**-Tab.
- Annahme führt zu: Geld wird gutgeschrieben, Spieler wird aus dem Kader entfernt, `financeLedger`-Eintrag als `income / transfer`, Angebot wird gelöscht, `accepted` in Log.
- Ablehnung oder Ablauf: Angebot wird gelöscht, Spieler bleibt im Kader, `rejected` bzw. `expired` in Log.

---

## Liga-Struktur — woher kommen Käufer-Clubs

`leagueWorld.js` hat eine Dreiliga-Struktur (`liga1`, `liga2`, `liga3`), je 18 Teams. Die `games.teams` enthält alle vereine, wobei der Benutzer ein spezifisches Team aus `liga3` hat. Käufer-Clubs werden aus allen Teams desselben Ligazweigs gezogen (also alle `liga1`-Teams, falls der Benutzer in liga1 spielt) — ohne Reserve-Teams. Angebote aus höheren Ligen sind durchweg unwahrscheinlich, niedrigeren Ligen hohe Wahrscheinlichkeit — dies wird für spätere Iteration reserviert, aktuell nur gleiche Liga.

---

## Storage-Compatibilität

Es gibt eine bestehende Validierungsfunktion `storage.js` `isPlayer(value)`, die nur bekannte Felder prüft. Alte gespeicherte Spieler haben noch keine `marketValue` und `isOnTransferList`. Beim `normalizeSaveData()`-Aufruf müssen diese fehlenden Felder mit Fallback-Werten (`marketValue: 0`, `isOnTransferList: false`) belegt werden, damit keine Alt-Saves ungültig werden.

---

## Globale Helferfunktionen (neu in `src/domain/transferMarket.js`)

```js
export function addTransferMessage(text) { /* … */ }
export function buyPlayer(transferablePlayer) { /* … */ }
export function sellPlayer(playerId) { /* … */ }
export function cancelSell(playerId) { /* … */ }
export function acceptExternalOffer() { /* … */ }
export function rejectExternalOffer() { /* … */ }
export function generateTransferMarketPlayers() { /* … */ }
export function generateExternalOffer() { /* … */ }
export function processTransferMarketLoop() { /* … */ }
```

---

## Geplante Integration in Post-Match-Phase

`gameLoop.js` Funktion `continueAfterMatch()` erst die bereits vorhandenen Blockaufrufe, dann neu **`processTransferMarketLoop(state)`**. Das ist der einmalige, nicht-endliche Aufruf, sobald ein Spiel vorbei ist. Danach wird die Transfermarkt-Ansicht durch `gameState.transferMarket.externalOffer` ggf. ein Angebot anzeigen, bis der Spieler reagiert oder es verfällt.

---

## Screen-IDs / `data-testid`-Werte

| Screen | `currentScreen` | Datei | Haupt-`data-testid` |
|---|---|---|---|
| Transfermarkt Home | `"transferMarket"` | `transferMarketScreens.js` | `transfer-market-screen` |
| Spieler kaufen | `"transferMarket-buy"` | `transferMarketScreens.js` | `transfer-market-buy-screen` |
| Spieler verkaufen | `"transferMarket-sell"` | `transferMarketScreens.js` | `transfer-market-sell-screen` |

---

## Dateien zu erstellen

### 1. `src/domain/transferMarket.js`
Barrel-Re-Export aus `state/store.js`, nach Muster von `domain/youth.js`.

```js
export {
  addTransferMessage,
  acceptExternalOffer,
  buyPlayer,
  cancelSell,
  generateExternalOffer,
  generateTransferMarketPlayers,
  processTransferMarketLoop,
  rejectExternalOffer,
  sellPlayer,
  transferMarketState
} from "../state/store.js";
```

### 2. `src/views/transferMarketScreens.js`
Drei Export-Funktionen:

```js
export function renderTransferMarketHomeScreen(state) { /* SCREEN 1 */ }
export function renderTransferMarketBuyScreen(state) { /* SCREEN 3 */ }
export function renderTransferMarketSellScreen(state) { /* SCREEN 2 */ }
```

**SCREEN 1** (`renderTransferMarketHomeScreen`):
- `mobile-shell--roster`, `data-testid="transfer-market-screen"`
- Header: `eyebrow = "Transfermarkt"`, `<h1>${state.clubName}</h1>`, `<p>Spieler kaufen und verkaufen</p>`
- Zwei Buttons nebeneinander in einem Container: `data-action="show-transfer-buy"` ("Kaufen") und `data-action="show-transfer-sell"` ("Verkaufen")
- Message-Box darunter: rendert `state.transferMessages` (max. 5 Einträge, neueste oben); bei vorhandenem `externalOffer` zusätzliche Buttons "Annehmen" / "Ablehnen"
- Zurück-Knopf: `data-action="back-club"`

**SCREEN 2** (`renderTransferMarketSellScreen`):
- `section class="screen screen--club screen--transfer-market"`, `mobile-shell--roster`, `data-testid="transfer-market-sell-screen"`
- Header: `club-header club-header--roster glossy-panel` → `eyebrow = "Verkaufen"`, Club-Name, `<p>Spieler zum Verkauf anbieten</p>`
- Zwei `glossy-panel`-Sektionen nach Muster von `renderRosterGroup()` in `rosterView.js`: **"Startelf"** und **"Bank"**
- Jede Spielerzeile: Name, Alter, Stärke + Knopf `"Verkaufen"` (`data-action="sell-player"`, `data-player-id`)
- Bottom: `button class="action-button action-button--blue roster-action" data-action="back-transfer"` → `"Zurück"`

**SCREEN 3** (`renderTransferMarketBuyScreen`):
- `section class="screen screen--club screen--transfer-market"`, `mobile-shell--roster`, `data-testid="transfer-market-buy-screen"`
- Header: `club-header club-header--roster glossy-panel` → `eyebrow = "Kaufen"`, Club-Name, `<p>Spieler von anderen Vereinen kaufen</p>`
- Message-Box (gleiche `glossy-panel` wie SCREEN 1) mit Log-Einträgen
- Liste von max. 5 Spielern (Name, Alter, Stärke, Ablöse), jede Zeile mit Knopf `"Kaufen"`
- Bottom: `button class="action-button action-button--blue roster-action" data-action="back-transfer"` → `"Zurück"`

**SCREEN 1 Detail** (`renderTransferMarketHomeScreen`):
- `section class="screen screen--club screen--transfer-market"`, `mobile-shell--roster`, `data-testid="transfer-market-screen"`
- Header: `club-header club-header--roster glossy-panel` → `eyebrow = "Transfermarkt"`, Club-Name, `<p>Spieler kaufen und verkaufen</p>`
- Message-Box: `glossy-panel`, rendert `state.transferMessages` (max. 5 Einträge, neueste oben); bei `externalOffer` zusätzliche Buttons-Annahme/Ablehnung-Zeile
- Zwei Buttons nebeneinander in einem `roster-actions`-Container (`"Verkaufen"` → `setScreen("transferMarket-sell")`, `"Kaufen"` → `setScreen("transferMarket-buy")`)
- Bottom: `button class="action-button action-button--blue roster-action" data-action="back-club"` → `"Zurück"` (rückwärts zu Club Hub)

```css
/* verwendet ausschliesslich bereits bestehende CSS-Klassen */
```

Die drei neuen Screens nutzen **keine neuen CSS-Klassen** (ausser der Screen-Unterscheidung `screen--transfer-market`-, die lediglich eine vorhandene Backdrop-Regel greifen lässt falls gewünscht). Alle strukturellen Klassen stammen aus der bestehenden Codebase:
`screen`, `screen--club`, `screen__backdrop`, `mobile-shell`, `mobile-shell--roster`, `club-header`, `club-header--roster`, `glossy-panel`, `eyebrow`, `training-feed`, `roster-group`, `roster-row`, `roster-actions`, `action-button`, `action-button--blue`, `action-button--green`, `formation-selector`, `formation-selector__buttons`.

Nur für die **Screen-Hintergrund-Differenzierung** ggf. neue Backdrop-Regeln ergänzt:
```
.screen--transfer-market .screen__backdrop { /* optional, gleiches Muster wie andere */ }
```

---

## Dateien zu ändern

### 4. `src/config.js`

- `inactiveTiles` um `"Transfermarkt"` erweitern:
  ```js
  inactiveTiles: ["Transfermarkt", "Kader", "Training", "Jugend", "Stadion"]
  ```

### 5. `src/views/clubView.js`

- In `createTileMarkup(label, state)` neuen `if`-Zweig für `"Transfermarkt"` hinzufügen → rendert Button mit `data-action="show-transfermarket"`, `data-testid="transfer-market-tile"`.
- Format: gleiches Kachel-Layout wie bei den anderen Tiles (Icon-Buchstabe "T", Label "Transfermarkt").

### 6. `src/state/store.js`

#### `isPlayer(value)` — Validierung erweitern
```js
&& (typeof value.marketValue === "undefined" || Number.isFinite(value.marketValue))
&& (typeof value.isOnTransferList === "undefined" || typeof value.isOnTransferList === "boolean")
```

#### `normalizePlayer(...)` — Felder hinzufügen
```js
marketValue: Number.isFinite(player?.marketValue)
  ? player.marketValue
  : fallbackPlayer.marketValue ?? 0,
isOnTransferList: typeof player?.isOnTransferList === "boolean"
  ? player.isOnTransferList
  : fallbackPlayer.isOnTransferList ?? false,
```

#### `createInitialState()` — neue Top-Level-Felder
```js
transferMarket: null,          // { externalOffer: null | {...}, availablePlayers: [] }
transferMessages: [],          // Array<{key, text}>, max 5 Einträge
```

#### Neue Funktionen in `state/store.js`

```js
// Erstellt einen Log-Eintrag und fügt ihn vorne an (max. 5)
export function addTransferMessage(entry) { ... }

// Berechnet Marktwert aus Stärke: strength² × 100, gerundet auf 1000
function computeMarketValue(strength) { ... }

// Normalisiert einen "kaufbaren" Spieler (Marktwert berechnen)
export function createTransferablePlayer(raw) { ... }

// Gibt bis zu 5 zufällige Spieler von Vereinen der gleichen Liga zurück
export function generateTransferMarketPlayers(state = gameState) { ... }

// Generiert ein externes Angebot (10 % pro Spieltag)
export function generateExternalOffer(state = gameState) { ... }

// Kauft einen Spieler vom Markt
export function buyPlayer(transferablePlayer, state = gameState) { ... }

// Setzt einen eigenen Spieler auf die Transferliste
export function sellPlayer(playerId, state = gameState) { ... }

// Nimmt einen Spieler von der Transferliste
export function cancelSell(playerId, state = gameState) { ... }

// Nimmt ein externes Angebot an
export function acceptExternalOffer(state = gameState) { ... }

// Lehnt ein externes Angebot ab
export function rejectExternalOffer(state = gameState) { ... }

// Post-Match-Loop-Tick: prüft auf abgelaufene Angebote, generiert ggf. neues
export function processTransferMarketLoop(state = gameState) { ... }
```

### 7. `src/ui/events.js` — Bindings hinzufügen

```js
import {
  // ... bestehende imports ...
  addTransferMessage,
  acceptExternalOffer,
  buyPlayer,
  cancelSell,
  generateTransferMarketPlayers,
  rejectExternalOffer,
  sellPlayer
} from "../transferMarket.js";

// SCREEN 1: Home
document.querySelector('[data-action="show-transfermarket"]')?.addEventListener("click", () => setScreen("transferMarket"));
document.querySelector('[data-action="show-transfer-buy"]')?.addEventListener("click", () => {
  generateTransferMarketPlayers(gameState);
  setScreen("transferMarket-buy");
});
document.querySelector('[data-action="show-transfer-sell"]')?.addEventListener("click", () => setScreen("transferMarket-sell"));
document.querySelector('[data-action="accept-offer"]')?.addEventListener("click", () => acceptExternalOffer(gameState));
document.querySelector('[data-action="reject-offer"]')?.addEventListener("click", () => rejectExternalOffer(gameState));

// SCREEN 2: Verkaufen
document.querySelector('[data-action="back-transfer"]')?.addEventListener("click", () => setScreen("transferMarket"));
document.querySelectorAll('[data-action="sell-player"]').forEach(btn => {
  btn.addEventListener("click", () => sellPlayer(btn.dataset.playerId, gameState));
});

// SCREEN 3: Kaufen
document.querySelectorAll('[data-action="buy-player"]').forEach(btn => {
  btn.addEventListener("click", () => buyPlayer(JSON.parse(btn.dataset.playerJson), gameState));
});
```

### 8. `src/main.js`

- Import hinzufügen:
  ```js
  import {
    renderTransferMarketBuyScreen,
    renderTransferMarketHomeScreen,
    renderTransferMarketSellScreen
  } from "./views/transferMarketScreens.js";
  ```

- Render-Switch erweitern:
  ```js
  if (gameState.currentScreen === "transferMarket") {
    markup = renderTransferMarketHomeScreen(gameState) || renderClubScreen(gameState);
  } else if (gameState.currentScreen === "transferMarket-buy") {
    markup = renderTransferMarketBuyScreen(gameState) || renderTransferMarketHomeScreen(gameState);
  } else if (gameState.currentScreen === "transferMarket-sell") {
    markup = renderTransferMarketSellScreen(gameState) || renderTransferMarketHomeScreen(gameState);
  }
  ```

### 9. `src/storage.js`

- `isPlayer(value)`: `marketValue` und `isOnTransferList` zur Validierung hinzufügen
- `isTransferMarketPlayer(value)`: neue Validator-Funktion für kaufbare Spieler
- Beim Laden von Alt-Saves (`normalizeSaveData`): fehlende `marketValue` und `isOnTransferList` mit Standardwerten belegen (Migration)

---

## Game-Loop-Integration

In `gameLoop.js` → `continueAfterMatch()` im `setState`-Callback, NACH den bereits vorhandenen Blockaufrufen:

```js
processTransferMarketLoop(state);   // NEU
```

`processTransferMarketLoop(state)` pro Spieltag:
1. Prüft, ob ein vorhandenes Angebot abgelaufen ist → schreibt `expired`-Eintrag, löscht Angebot
2. Mit 10 % Wahrscheinlichkeit, falls kein aktives Angebot existiert, `generateExternalOffer(state)` aufrufen
```
