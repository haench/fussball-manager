# Refactoring-Plan: Projektstruktur stabilisieren

## Status: MAJOR WORK COMPLETED - Final Steps Remaining

### Already Completed (Steps 1-7):
1. ✅ **Step 1 - Safety Net & Smoke Tests**: `scripts/smoke.mjs` exists with league initialization, matchday completion, finance ledger, and match minute tests
2. ✅ **Step 2 - UI Helpers**: `src/ui/formatters.js` and `src/ui/viewHelpers.js` created
3. ✅ **Step 3 - Views Split**: All 8 view files created (`startView.js`, `clubView.js`, `rosterView.js`, `facilityViews.js`, `stadiumView.js`, `leagueViews.js`, `matchView.js`, `reportViews.js`)
4. ✅ **Step 4 - Event Binding**: `src/ui/events.js` extracted with `bindEvents()` function
5. ✅ **Step 5 - State Domain Modules**: `src/state/store.js` with domain re-exports via `src/domain/*.js` files
6. ✅ **Step 6 - Team Strength Centralized**: `src/domain/teamStrength.js` with `calculateTeamStrength` and `getStarterPlayers`
7. ✅ **Step 7 - Match Simulation Split**: `src/match/` directory with `matchState.js`, `resultEngine.js`, `animationEngine.js`, `resultSummary.js`, `helpers.js`, `movement.js`

### Remaining Work

---

## Step 8: Encoding & Text Resource Cleanup (URGENT)

### Problem Analysis
Multiple files contain mojibake (encoding corruption). Common patterns:
- `â‚¬` should be `€`
- `Ã¼` should be `ü`  
- `ÃŸ` should be `ß`
- `ðŸ‘¥` should be `👥`
- Broken UTF-8 in player names like `Krüger`

### Files Affected (by grep search):
1. `src/state/store.js` - line 67: `Emil KrÃ¼ger`
2. `src/state/store.js` - line 519: `stärker`
3. `src/state/store.js` - line 779: `Süd`
4. `src/views/clubView.js` - lines 87, 92: `mÃ¼ssen`, `gewÃ¤hlten`
5. `src/views/clubView.js` - line 204, 210: `NÃ¤chstes`
6. `src/views/rosterView.js` - line 56: `StÃ¤rke`
7. `src/views/rosterView.js` - line 101: `ZurÃ¼ck`
8. `src/views/facilityViews.js` - multiple occurrences
9. `src/views/leagueViews.js` - line 121: `Â·`
10. `src/views/stadiumView.js` - lines 21, 113, 119, 129, 144
11. `src/views/reportViews.js` - line 92, 93: `Ã„nderung`
12. `src/views/matchView.js` - lines 39, 86, 173, 174: `fÃ¼r`, `TorschÃ¼sse`

### Action Items (Execute in order):
1. Fix `src/state/store.js` - correct player name and German umlauts in status messages
2. Fix `src/views/clubView.js` - correct warning messages and headers
3. Fix `src/views/rosterView.js` - correct strength label and back button
4. Fix `src/views/facilityViews.js` - correct all German text
5. Fix `src/views/leagueViews.js` - fix · separator
6. Fix `src/views/stadiumView.js` - fix ticket prices and labels
7. Fix `src/views/reportViews.js` - fix Änderung
8. Fix `src/views/matchView.js` - fix Tor and Torschüsse

---

## Step 9: Styles Organization

### Current State
`src/styles.css` is 1722 lines with no section organization comments.

### Action Items:
1. Add section header comments:
   - `/* Base */`
   - `/* Layout */`
   - `/* Buttons */`
   - `/* Club */`
   - `/* Roster */`
   - `/* Facilities */`
   - `/* Stadium */`
   - `/* League */`
   - `/* Match */`
   - `/* Reports */`
   - `/* Responsive */`
2. Group existing rules under appropriate sections (no functional changes)

---

## Step 10: Extended Tests

### Current Smoke Tests (in `scripts/smoke.mjs`):
- League initialization (3x18 teams)
- Matchday structure (34 days, 9 games per day)
- Matchday completion
- Finance ledger summing
- Match minute simulation (5 minutes)

### Additional Tests to Add:
1. **Table Sorting Test**: Verify standings sort by points, goal diff, goals for
2. **Formation Validation Test**: Test `isLineupValid` with correct/incorrect formations
3. **Match Result Test**: With `matchDuration=1`, verify goals are generated and report is created
4. **State Persistence Test**: Create game, modify state, verify save/load

---

## Verification Commands

After each step, run:
```bash
npm run build    # Verify no syntax errors
npm run smoke    # Verify all tests pass
```

---

## Execution Order

1. Fix encoding issues (Step 8) - highest priority, affects user experience
2. Organize CSS (Step 9) - cosmetic but improves maintainability  
3. Add tests (Step 10) - ensure nothing is broken during encoding fixes

---

## Risk Assessment

The encoding fixes are purely text replacements with no logic changes. The game behavior remains identical, only the text display improves.